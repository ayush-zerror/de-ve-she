/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['dieselfarm.com','emmpo.com',"rahasyafragrances.com","www.datocms-assets.com"],
  },
};

export default nextConfig;
